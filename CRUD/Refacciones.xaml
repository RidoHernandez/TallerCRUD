<Window x:Class="CRUD.Refacciones"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRUD"
        mc:Ignorable="d"
        Title="Refacciones" Height="800" Width="1000">
    <Grid>
        <!--<Image HorizontalAlignment="Left" Height="430" Margin="0,1,0,0" VerticalAlignment="Top" Width="313" Style="{StaticResource img-banner3}"/>-->
        <!-- PANEL IZQUIERDO -->
        <Border Style="{StaticResource LeftPanel}" Margin="12,57,588,77">
            <Grid Margin="20,5,20,20">
                <StackPanel Margin="20">

                    <Separator Margin="0,0,0,5" Opacity="0.4"/>

                    <!-- CAMPOS -->
                    <TextBlock Text="Nombre" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtNombre" Style="{StaticResource InputTextBox}"/>

                    <TextBlock Text="Marca" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtMarca" Style="{StaticResource InputTextBox}"/>

                    <TextBlock Text="Precio Unitario ($)" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtPrecio" Style="{StaticResource InputTextBox}"/>

                    <TextBlock Text="Stock Actual" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtStockActual" Style="{StaticResource InputTextBox}"/>

                    <TextBlock Text="Stock Mínimo" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtStockMinimo" Style="{StaticResource InputTextBox}"/>

                    <TextBlock Text="Proveedor" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtProveedor" Style="{StaticResource InputTextBox}"/>

                    <TextBlock Text="Código Refacción (Auto)" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="txtCodigo" Style="{StaticResource InputTextBox}" IsReadOnly="True"/>

                    <!-- BOTONES CRUD -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">

                        <Button x:Name="btnAgregar"
                                Content="Agregar"
                                Style="{StaticResource BtnPrimary}"
                                Click="btnAgregar_Click"/>

                        <Button x:Name="btnEditar"
                                Content="Editar"
                                Style="{StaticResource BtnWarning}"
                                Click="btnEditar_Click"/>

                        <Button x:Name="btnEliminar"
                                Content="Eliminar"
                                Style="{StaticResource BtnDanger}"
                                Click="btnEliminar_Click"/>

                    </StackPanel>

                    <Button x:Name="btnLimpiar"
                            Content="Limpiar Campos"
                            Style="{StaticResource BtnSecondary}"
                            Margin="0,10,0,0"
                            Click="btnLimpiar_Click"/>

                    <Button x:Name="btnRegresar"
                            Content="⬅ Regresar"
                            Style="{StaticResource BtnBack}"
                            Margin="0,5,0,0"
                            Click="btnRegresar_Click"/>

                </StackPanel>
            </Grid>
        </Border>

        <!-- PANEL DERECHO -->
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ENCABEZADO -->
            <TextBlock Text="Listado de Refacciones"
                       Style="{StaticResource HeaderText}"
                       Grid.Row="0" Margin="45,18,17,53"/>

            <!-- FILTROS -->
            <Border Style="{StaticResource FilterPanel-rec}" Margin="393,34,7,650" Grid.RowSpan="3">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Buscar por Nombre" Style="{StaticResource LabelFilter-rec}"/>
                        <TextBox x:Name="txtFiltroNombre" Style="{StaticResource FilterTextBox-rec}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Buscar por Marca" Style="{StaticResource LabelFilter}"/>
                        <TextBox x:Name="txtFiltroMarca" Style="{StaticResource FilterTextBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="10,0,0,0">
                        <TextBlock Text="Buscar por Proveedor" Style="{StaticResource LabelFilter}"/>
                        <TextBox x:Name="txtFiltroProveedor" Style="{StaticResource FilterTextBox}"/>
                    </StackPanel>

                    <Button Grid.Column="3"
                            Content="🔍 Buscar"
                            Style="{StaticResource BtnSearch}"
                            Margin="12,21,3,19"
                            Click="btnBuscar_Click"/>
                </Grid>
            </Border>

            <!-- DATAGRID -->
            <Border Grid.Row="2" Style="{StaticResource TableContainer}" Margin="409,54,31,-15">
                <DataGrid x:Name="dgRefacciones"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="dgRefacciones_SelectionChanged"
                          Style="{StaticResource CustomDataGrid}" Margin="-1,-3,14,137">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding Codigo_refaccion}" Width="80"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="150"/>
                        <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="120"/>
                        <DataGridTextColumn Header="Precio" Binding="{Binding Precio_unitario}" Width="90"/>
                        <DataGridTextColumn Header="Stock Actual" Binding="{Binding Stock_actual}" Width="110"/>
                        <DataGridTextColumn Header="Stock Mínimo" Binding="{Binding Stock_minimo}" Width="110"/>
                        <DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor}" Width="*"/>
                    </DataGrid.Columns>

                </DataGrid>
            </Border>

        </Grid>
    </Grid>
</Window>
